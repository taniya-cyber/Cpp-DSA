#include<iostream>
int main(){
// "()" This is known as Casting operator using in Manual Type Casting.

//Implicit Type Casting:
// char a = 'A';
// int b = a;
// std::cout<< b + 1;

// int q = 10;
// int w = 12;
// int /*This is implicit conversion, as the answer was in float which gets into int.*/ result = (float)q /*this is explicit type conversion; which is done to print result in float without changing datatype of origina.*/ / w;
// std::cout<< result;
    return 0;
}